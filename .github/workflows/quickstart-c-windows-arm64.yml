# Badge + manual dispatch for Windows arm64 only (delegates to quickstart-c.yml).
name: quickstart-c Windows arm64

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  run:
    uses: ./.github/workflows/quickstart-c.yml
    with:
      build_platform: windows-arm64
    secrets: inherit
