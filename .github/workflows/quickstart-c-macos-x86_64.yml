# Badge + manual dispatch for macOS x86_64 only (delegates to quickstart-c.yml).
name: quickstart-c macOS x86_64

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  run:
    uses: ./.github/workflows/quickstart-c.yml
    with:
      build_platform: macos-x86_64
    secrets: inherit
