# Badge + manual dispatch for Linux arm64 only (delegates to quickstart-c.yml).
name: quickstart-c Linux arm64

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  run:
    uses: ./.github/workflows/quickstart-c.yml
    with:
      build_platform: linux-arm64
    secrets: inherit
